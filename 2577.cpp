#include <iostream>
using namespace std;

int	main(void)
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int	A, B, C;
	cin >> A >> B >> C;
	while ((A * B * C) / 10 != 0)
	{
		tmp = (A * B * C) % 10
		tmp = (A * B * C) / 10;
	}
	return (0);
}
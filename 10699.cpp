#include <bits/stdc++.h>
#define fast_io ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
using namespace std;

void solve()
{
	long n, m;
	cin >> n >> m;
	cout << abs(n - m);
}

int main(void)
{
	fast_io;
	solve();
	return 0;
}
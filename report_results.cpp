#include <string>
#include <vector>
#include <iostream>
#include <sstream>
#include <map>

using namespace std;

map<string, int> id_index_map;

vector<int> solution(vector<string> id_list, vector<string> report, int k) {
    vector<int> answer;

	map<string, string> report_id;

	for (auto i : id_index_map)
		id_index_map[i] = i;

	for (auto rep : report)
	stringstream str();


    return answer;
}